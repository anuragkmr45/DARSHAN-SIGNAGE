# Deployment Guide Moved

The canonical player deployment and environment runbook now lives in the `DARSHAN monorepo root` repo:

`docs/runbooks/player-deployment.md`

This player repo keeps player build, packaging, runtime, and troubleshooting guidance. Use the platform repo for QA and production environment deployment.
