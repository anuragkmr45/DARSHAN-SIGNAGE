global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    site: dev-local
    environment: development

rule_files:
  - /etc/darshan/prometheus/rules/*.yml

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - 127.0.0.1:9090
        labels:
          role: observability
          machine: local

  - job_name: darshan-server
    metrics_path: /metrics
    authorization:
      type: Bearer
      credentials: darshan-development-prometheus
    static_configs:
      - targets:
          - host.docker.internal:3000
        labels:
          role: backend
          machine: vm2

  - job_name: vm2-node
    static_configs:
      - targets:
          - node-exporter:9100
        labels:
          role: backend
          machine: vm2

  - job_name: players
    metrics_path: /metrics
    file_sd_configs:
      - files:
          - /etc/darshan/prometheus/file-sd/players.targets.json
        refresh_interval: 1m
