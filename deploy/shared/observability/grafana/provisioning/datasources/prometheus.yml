apiVersion: 1

datasources:
  - name: DARSHAN Prometheus
    uid: darshan-prometheus
    type: prometheus
    access: proxy
    url: ${PROMETHEUS_UPSTREAM_URL}
    isDefault: true
    editable: false
