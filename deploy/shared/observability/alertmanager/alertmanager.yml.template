global:
  resolve_timeout: 5m

templates:
  - /etc/darshan/alertmanager/templates/*.tmpl

# Default deployment posture:
# - Alertmanager runs on the observability VM for grouping, silences, and alert inspection.
# - Outbound notification integrations are site-specific and must be added at render time.
# - The base template intentionally keeps alerts local-only until a site-specific receiver is configured.
route:
  receiver: darshan-local-only
  group_by:
    - alertname
    - severity
    - role
    - site
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: darshan-local-only

inhibit_rules:
  - source_matchers:
      - severity="critical"
    target_matchers:
      - severity="warning"
    equal:
      - alertname
      - instance
