# DARSHAN Design Discovery Code-Truth Traceability

Last code-truth refresh: 2026-06-28.

This file links product/design domains to the current codebase. Code is authoritative. Screenshots are evidence labels only and were not recaptured during this refresh.

## Evidence Policy

- Every feature row must cite code paths.
- Runtime-only behavior is marked `needs runtime verification`.
- Design recommendations must preserve the listed API, route, IPC, and runtime contracts.
- Existing screenshots are historical, after-redesign, final-QA, or DOM-only as labeled in `SCREENSHOT_INDEX.md`.

## Product Domain Traceability

| Feature / domain | Backend source of truth | CMS route / component source | Player source, if any | API / data dependency | Screenshot evidence | Design risk / notes |
|---|---|---|---|---|---|---|
| Auth and session | `darshan-server/src/routes/auth.ts`, auth plugins in `darshan-server/src/server/index.ts` | `/login` in `darshan-cms/src/App.tsx`, `darshan-cms/src/pages/Auth.tsx`, `darshan-cms/src/store/authSlice.ts`, `ProtectedRoute` | none | Auth endpoints from `darshan-server/src/config/apiEndpoints.ts`; CSRF/session behavior in API client | login screenshots in `screenshots/`, `after-redesign-screenshots/`, `final-qa-screenshots/` | High. Do not change auth/session, redirect, CSRF, or protected route behavior for visual work. |
| Dashboard and operational overview | metrics, reports, observability, screens, media, requests route modules registered in `darshan-server/src/server/index.ts` | `/dashboard`, dashboard components under `darshan-cms/src/components/dashboard/*` | telemetry and player health are upstream sources only | metrics, reports, screens, media, requests, observability API domains | dashboard screenshots in all screenshot sets | Moderate. Dashboard can reorganize existing data but must not invent metrics. |
| Media library and upload | `darshan-server/src/routes/media.ts`, S3/MinIO code, media processing/runtime tool checks | `/media`, `darshan-cms/src/pages/MediaLibrary.tsx`, `darshan-cms/src/api/domains/media.ts` | cache manager and playback renderers consume media URLs from snapshots/default media | media upload/presign/complete/delete, object storage | media screenshots | High. Preserve upload/delete contracts and HTTP/object-storage media transport. |
| Layouts and presentations | `darshan-server/src/routes/layouts.ts`, `routes/presentations.ts`, layout repositories/schema | `/layouts`, `/layouts/new`, `/layouts/:id`; layout editor and layout API domain | renderer layout helpers and snapshot parsing | layouts, presentations, media references | layouts screenshots | High. Layout geometry and composition are behavior, not just styling. |
| Schedule queue and creation | `darshan-server/src/routes/schedules.ts`, `schedule-requests.ts`, `schedule-reservations.ts`, `schedule-publish-helper.ts` | `/schedule`, `/schedule/new`, schedule queue/creator step components | `schedule-manager.ts`, `snapshot-manager.ts`, `playback-policy.ts`, renderer playback | schedules, requests, reservations, snapshots, publishes, commands | schedule queue/creator screenshots | High. Multi-step schedule and publish behavior must remain unchanged. |
| Screens, groups, fleet health | `darshan-server/src/routes/screens.ts`, `screen-groups.ts`, `device-telemetry.ts` | `/screens`, screen components including Pairing Health, details, groups, screenshot actions | heartbeat, screenshot, media cache report, telemetry, realtime services | screens, groups, heartbeat, cache reports, screenshots, delivery/desired-state | screens and pair-modal screenshots | High. Operational/destructive actions and online status must remain accurate. |
| Player pairing and recovery | `darshan-server/src/routes/device-pairing.ts`, pairing services/cert repositories | Pair Device modal and Pairing Health panel via `devicePairing` API domain | `pairing-service.ts`, `player-flow.ts`, `cert-manager.ts`, `device-state-store.ts`, `renderer/pairing.ts` | pairing request/status/complete/recovery, screen identity/certs | pair-device screenshots; player OTP DOM-only screenshots | High. Backend pairing-status remains authority; do not change protocol or local identity semantics. |
| Heartbeat and device online state | `darshan-server/src/routes/device-telemetry.ts`, telemetry jobs/services | Screens, dashboard, reports status panels | `telemetry/heartbeat.ts`, player metrics/system stats | heartbeat and telemetry endpoints, device/session tables | screens/dashboard screenshots | High. Online/offline is backend-observed and requires runtime verification. |
| Default media | settings/default-media logic in `darshan-server/src/routes/settings.ts` and related utilities | `/settings`, default media section/components and `settings` API domain | `settings/default-media-service.ts`, `default-media-player.ts` | settings/default media endpoints, desired-state/refresh behavior | settings default-media screenshots | High. Default media affects player fallback playback and realtime refresh. |
| Emergency and requests | `darshan-server/src/routes/emergency.ts`, `routes/requests.ts`, command refresh services | `/requests`, emergency modal/control surfaces, reports emergency summary | command processor, snapshot/default playback response to refresh | emergency, requests, command lifecycle | requests screenshots | High. Emergency controls need distinct visual treatment and preserved safety. |
| Command lifecycle and desired state | command services, outbox, desired-state, `device-telemetry.ts`, realtime gateway | delivery/cache/status panels in screens/dashboard | `command-processor.ts`, `realtime-service.ts`, polling fallback services | device commands, ACKs, desired-state, outbox, Valkey fanout | no direct screenshot; visible through screens/status UI | High. Socket.IO is notification-only; REST/DB source-of-truth must remain. |
| Chat and conversations | `darshan-server/src/routes/chat.ts`, `routes/conversations.ts`, browser realtime namespaces | `/chat*`, `/conversations*`, chat components and hooks | none | chat/conversations REST plus browser realtime hints | chat screenshots | High. Realtime hints supplement REST state and must not become source-of-truth. |
| Notifications | `darshan-server/src/routes/notifications.ts`, notification namespace | `/notifications`, notification hooks/components/header | none | notification REST plus browser realtime hints | notifications screenshots | Moderate. Preserve unread/mark-read/delete behavior. |
| Reports, audit, proof-of-play | `routes/reports.ts`, `routes/audit-logs.ts`, `routes/proof-of-play.ts`, PoP jobs/services | `/reports`, `/proof-of-play`, reports and PoP API domains | `pop-service.ts`, active playback IPC, request queue/spool | reports, audit logs, proof-of-play | reports and proof screenshots | Moderate. Evidence UI must not imply fake continuous playback after crash. |
| Settings, RBAC, admin | settings, roles, permissions, users, invites, departments, operators route modules | `/settings`, `/users`, `/operators`, `/departments` | player config only for environment labels and runtime behavior | settings, users, roles, permissions, departments | settings screenshots | High. RBAC/default media/security settings can affect runtime behavior. |
| Integrations | `api-keys.ts`, `webhooks.ts`, `sso-config.ts` | `/api-keys`, `/webhooks`, `/sso-config` | none | API keys, webhooks, SSO config | no dedicated screenshot listed | High. Secret-like values and auth config require careful visual handling. |
| Observability | `routes/metrics.ts`, `routes/observability.ts`, Prometheus metrics assets | dashboard/reports/observability panels and runtime config | player metrics/logs/health support runtime evidence | metrics/observability endpoints and Prometheus/Grafana deployment | dashboard/reports screenshots | Moderate. Runtime scrape health is not proven by docs. |
| Player playback and renderer states | device telemetry/snapshot/default media endpoints | CMS schedule/media/screens/default-media flows configure playback | `renderer/player.ts`, `default-media-player.ts`, `pdf-playback.ts`, `webpage-playback.ts`, `playback-policy.ts`, `playback-progress-store.ts` | snapshot/default-media/media URLs/local cache/PoP | player OTP DOM-only only; no live playback screenshot in this refresh | High. Renderer behavior needs target-device testing for video/PDF/office/webpage. |
| Player secure/offline/restart behavior | pairing-status, device telemetry, command routes | screens/diagnostics show effects | `secure-playback-guard.ts`, `offline-security-policy.ts`, `player-flow.ts`, `device-state-store.ts`, `request-queue.ts`, `playback-progress-store.ts` | pairing validation, offline grace, local state, queues | no fresh runtime screenshot | High. Needs runtime verification for shutdown, network loss, recovery, and target hardware. |
| Deployment and config | backend config loader, runtime bootstrap, deploy Docker scripts/docs | CMS runtime config loader and nginx runtime config | player file config loader and packaged player config | env selectors, JSON config files, Docker role env | no screenshot evidence | High. Production readiness is not claimed by design discovery. |

## Traceability Gaps

| Gap | Why it remains open |
|---|---|
| Live Electron OTP and playback screenshots | Only DOM-only player screenshots are indexed; packaged/live Electron evidence was not recaptured. |
| Real production Docker health | This refresh did not run five-VM Docker deployment or health probes. |
| Target-device media rendering | PDF/office/webpage/video behavior depends on packaged player, OS, codecs, runtime tools, cache, and backend media URLs. |
| Realtime latency | Socket.IO and polling fallback behavior require live backend, Valkey, CMS, and player runtime evidence. |
| Screenshots currentness | Existing screenshots are historical/final-QA labels and should be recaptured before the next UI implementation phase. |

